# Hello Plugin

1. Create class implementing `GigaPlugin`.
2. Build a DSL delegate with `gigaPlugin("your-id") { ... }`.
3. Add `gigaplugin.yml` with `main` class.
4. Build jar and place it in `plugins/GigaSoftBridge/giga-plugins/`.
5. Run `/giga reload all`.
