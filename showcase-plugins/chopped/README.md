# Showcase: Chopped

Commands:
- `chopped-status`
- `chopped-config [maxNodes] [maxDelayTicks]`

Behavior:
- Break one log, then the whole connected tree breaks with a delay.
- Delay scales with tree size.
- Configurable scan/defer limits.
