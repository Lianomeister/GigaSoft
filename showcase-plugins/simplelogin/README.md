# Showcase: SimpleLogin

Commands:
- `register <password> <password>`
- `login <password>`
- `logout`
- `changepassword <old> <new> <new>`
- `login-status [player]`

Behavior:
- First join registration flow.
- Password login on next joins.
- Password hashes with random salt.
